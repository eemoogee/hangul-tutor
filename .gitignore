__pycache__/
*.pyc
data/user_progress.json
commit_message.txt
.venv_finetune/
hangul_expert_model/
outputs/
unsloth_compiled_cache/
*.log
data/user_progress.backup-*.json
production_probe_raw.jsonl
curriculum_probe_raw.jsonl
pedagogy_probe_raw.jsonl

# Session verification/test scripts
_verify_*.py
test_*.py
# Exceptions: committed verifiers/probes that must survive a clean clone.
# Keep these AFTER the ignore patterns above — a negation only works if it
# follows the rule it overrides.
!scripts/_verify_*.py
!scripts/_probe_*.py

# Session notes and scratch files
9-22_hangul-tutor-gamplay-test.md
Claude outputs/
Modelfile_systemprompt
generate_wrong_attempt_pairs.py
data/deo-do-entry.json

# Fine-tune artifacts (abandoned track)
hangul_finetune_v13.jsonl
production_probe_raw_*.jsonl

# Build output
dist/

# Backup files (alternate naming convention)
data/user_progress.json.bak-*

# Additional backup naming convention
data/user_progress.backup-*.json

# Fine-tune data (all versions)
hangul_finetune_v*.jsonl

# Claude Code working directory
.claude/

